export default function ResourceFilters({ query, onQuery, category, onCategory, type, onType, status, onStatus, categories, types }) {
  return <section className="resource-tools" aria-label="Buscar y filtrar recursos"><label className="search-label"><span>Buscar</span><input type="search" value={query} onChange={(event) => onQuery(event.target.value)} placeholder="Ej. entrevista, CV, inglés…" /></label><div className="filter-group"><span>Categoría</span><div className="filter-row">{['Todos', ...categories].map((item) => <button key={item} type="button" className={category === item ? 'active' : ''} onClick={() => onCategory(item)}>{item}</button>)}</div></div><div className="compact-filters"><label>Tipo<select value={type} onChange={(event) => onType(event.target.value)}><option>Todos</option>{types.map((item) => <option key={item}>{item}</option>)}</select></label><label>Estado<select value={status} onChange={(event) => onStatus(event.target.value)}><option>Todos</option><option value="disponible">Disponible</option><option value="proximamente">Próximamente</option></select></label></div></section>
}
